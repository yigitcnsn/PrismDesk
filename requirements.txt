# Core (works with pip opencv OR system python3-opencv on Pi)
numpy>=1.24.0
PyYAML>=6.0
Pillow>=10.0.0

# Photo measure (HEIC). Optional on Pi if you only use live USB / JPEG.
pillow-heif>=0.13.0; platform_machine != "aarch64"

# Desktop / Mac pip OpenCV (on Raspberry Pi OS prefer: sudo apt install python3-opencv)
opencv-python>=4.8.0; platform_machine != "aarch64"

# Hand tracking
mediapipe>=0.10.9
